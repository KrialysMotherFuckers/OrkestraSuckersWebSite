@page "/account"

<Title HeaderTitle="@Trad.Keys["Account"]"></Title>

<MudTabs Elevation="25" Color="Color.Transparent" Rounded="true" PanelClass="mt-6">
    <MudTabPanel Text="@Trad.Keys["Profile"]">
        <Profile />
    </MudTabPanel>
    @if (!SecurityTabHidden)
    {
        <MudTabPanel Text="@Trad.Keys["Security"]">
            <Security />
        </MudTabPanel>
    }
    <MudTabPanel Text="@Trad.Keys["Claims"]">
        <Claims />
    </MudTabPanel>
</MudTabs>

@code
{
    //[Inject]
    //public IAuthenticationService AuthService { get; set; } = default!;

    public bool SecurityTabHidden { get; set; } = false;

    protected override void OnInitialized()
    {
        //if (AuthService.ProviderType == AuthProvider.AzureAd)
        //{
        //    SecurityTabHidden = true;
        //}
    }
}